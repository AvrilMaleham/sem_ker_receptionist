[project]
name = "myproject"
version = "0.1.0"
description = "An agentic receptionist project"
requires-python = ">=3.12"

dependencies = [
  "pydantic",
  "pydantic[email]",
  "python-dotenv",
  "semantic-kernel[all]",
  "pydantic-extra-types",
  "asyncpg",
  "openai"
]


